import React from "react";
import { Layout } from 'antd';

const { Nav, Footer, Sider, Content } = Layout;
const Header = () => {


    return (
        <Layout style={{height: 100}}>
            <h1>Dashboard</h1>
        </Layout>
    )
}

export default Header
